let FirstArray = ['benz', 'bmw', 'mvm'];
let SecondArray = new Array('car', 'home', 30);
console.log(FirstArray, SecondArray);

const cartItems = ['books1', 'books2', 'books3', 'books4'];
for (let count = 0; count < cartItems.length; count++) {
    console.log(cartItems[count]);
}